{
  "name": "atomic",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "watch": "pnpm tailwindcss -o dist/out.css -w",
    "build": "pnpm tailwindcss -o dist/out.css --minify",
    "lint": "eslint --fix {atoms,molecules,organisms}/**/*.{ts,tsx}"
  },
  "devDependencies": {
    "eslint-config-custom": "workspace:*",
    "tsconfig": "workspace:*"
  },
  "dependencies": {
    "next": "14.0.2"
  }
}
